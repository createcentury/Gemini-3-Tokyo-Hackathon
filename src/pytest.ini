[pytest]
# テストディレクトリ
testpaths = tests

# 詳細出力
addopts =
    -v
    --tb=short
    --strict-markers

# マーカー定義
markers =
    unit: ユニットテスト（外部依存なし）
    integration: 統合テスト（TestClientなど）
    slow: 処理に時間がかかるテスト

# テストの検出パターン
python_files = test_*.py
python_classes = Test*
python_functions = test_*
